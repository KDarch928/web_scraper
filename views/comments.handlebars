<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <a href="{{url}}">
                        <h5 class="card-title">{{title}}</h5>
                    </a>
                    <p class="card-text">
                        {{summary}}
                    </p>
                    <br>
                    <form action="/" method="GET">
                        <button type="submit" class="btn btn-info">Back</button>
                    </form>
                 </div>
            </div>

        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            {{#if comment}}
                <div class="card">
                    <div class="card-body">
                        <p class="card-text">
                            {{comment.title}}
                        </p>
                    </div>>
                </div>
            {{/if}}
        </div>
    </div>

</div>